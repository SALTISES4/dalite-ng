[pytest]
addopts = --durations 10 --reuse-db --ignore=node_modules/ --ignore=functional_tests/ -x -s
testpaths =
  blink/
  dalite/
  peerinst/
  quality/
  reputation/
  REST/
  tos/
DJANGO_SETTINGS_MODULE = dalite.test_settings
filterwarnings =
